spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/sistema
    username: admin
    password: admin

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

api:
  security:
    token:
      secret: minha-chave-super-secreta-para-jwt-2026
      expiration: 86400000